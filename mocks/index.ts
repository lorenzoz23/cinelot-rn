import { mockLotCollection, mockWishlistCollection } from "./MovieCollection";
import { mockPrizedPossessions } from "./PrizedPossessions";

export { mockLotCollection, mockWishlistCollection, mockPrizedPossessions };
