import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    borderColor: "#FF33C7",
    borderWidth: 3,
    borderRadius: 50,
  },
  image: {
    width: 60,
    height: 60,
    borderRadius: 50,
  },
});
